<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <% include ./partials/head.ejs %>
  </head>
  <body>
    <header>
      <img class="logo" src="/images/logo.jpg" alt="logo"/>
    </header>
    <h1>Available Pets</h1>
    <ul>
      <% for(let i=0; i < pets.length; i++) { %>
        <li>
          <img src="/images/<%=pets[i].img%>" alt="animal image">
        </br>
          There are <%=pets[i].qty%> <a href="/pets/<%=pets[i].id%>"><%=pets[i].breed%></a> <%=pets[i].species%> that cost $<%=pets[i].price%>. The care rating is <%=pets[i].rating%>. <% if(pets[i].readyToGo === true) { %>
          This pet is available to come home with you today!
          <% } else { %>
            There is a waiting period before this animal can head home!
            <% } %>
            <form action="/pets/<%=pets[i].id%>?_method=DELETE" method="POST">
              <input type="submit" value="DELETE"/>
            </form>
            <button type="button">
              <a href="/pets/<%=pets[i].id%>/edit">Edit Your Pet Choice</a>
            </button>
          </br>
          </br>
        </li>
      <% } %>
    </ul>
    <div class=container>
      <nav>
        <% include ./partials/nav.ejs %>
      </nav>
    </div>
  </body>
</html>
